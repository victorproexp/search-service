version: '3.7'
services:
  SearchApi:
      image: search-api:latest
      expose:
      - "80"
  nginx:
      image: nginx:latest
      volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      depends_on:
      - SearchApi
      ports:
      - "4000:4000"